
%% This code is for displaying the life span of different initial values

prime = 3;

count_lst = [];

for value = (1:1:20000)
    
    count_lst(value) = colatz_calculator(value, prime);

end

plot((1:1:length(count_lst)), mod(count_lst, prime), 'bo')
